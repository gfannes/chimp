- [x] Add support for CLI arguments that apply for every command:
	- Default reporting should use ANSI color output, the `--nocolor` flag must disable this.
- [x] Create a format to represent a filesystem in a single file. This format must be easy to read with a text editor. The goal is to use such filesystem-in-single-file to create different tests: an input file to describe a few groves, a chimp command and reference output expected from chimp, optionally combined with a output file for the export command. The format is called `naft` and is a bit similar to xml: a tag that describes the type of a certain node as `[tag]` (similar to xml `<tag>`), 0 or more primitive key/value options as `(key0:value0)(key1:value1)...` and support for nesting via `{}` (similar to the xml `</tag>`). Whitespace and newlines in-between these elements do not matter. `\`-based escaping must be used to allow the use of `)` in a key/value. This naft format must be used to represent a filesystem via the tags `[Folder](name:folder name)` and `[File](name:file name)(content:content of file)`. I want a basic CLI that supports encoding a set of folders into a single file and decoding a single file into a set of folders in some base folder. To actually define and drive the chimp end-to-end tests, we should decide to either implement these in Rust itself or in Ruby/Rake. Maybe this has some impact on the design of the naft app and requires the heart of the encoding and decoding to be available directly to the test system.
- [x] Create the `debug` command that shows all info on parsed files and aggregated metadata
- [x] Chores without an order must be reported first, then continue with Chores ordered from high order to small order
- [x] The order of a Chore is the minimum order of all Defs that relate to that Chore. All related Defs must be handled in a specified order and if one of them is _exclusive_, that order must be used and not the minimum. An order, in fact any piece of metadata, is exclusive if it contains the extra `^` marker: `&^#` iso `&#`. For other metadata items, the exclusive flag might also be useful that we will define later.
- [x] When a Chore is related to some AmpPath `&:a:b:c`, it is also related to `&:a:b` and `&:a`.
- [x] I want to change the formatting of `chimp chores`: each Chore line in the output must be color-coded according to the order. I want a `-d/--details` flag that will show additional metadata for each Chore: the minimal order, all related Defs, ...
- [x] Add support for reporting only the first `-n COUNT` Chores from `chimp chores`
- [x] Document the naft format. This acronym stands for: Nodes with Attributes, Free text and Tags.
- [x] Add a `-V/--verbose LEVEL` level flag, default 1.
- [x] For `chimp naft encode` on the chimp folder itself, I get `Error: Error { kind: InvalidData, message: "stream did not contain valid UTF-8" }`. I want to see more details on this error for verbose level 1. For verbose level 3, I want to see all the files that are processed.
- [x] `chimp naft encode` must skip hidden files/folders and take `.gitignore` into account. Only when `-u` is specified must hidden files be taken into account. Only when `-U` is specified must ignored files be taken into account.
- [x] Support non-UTF8 content with `chimp naft`
- [x] The readability of the naft filesystem output is not great: many escaped characters are present. For naft key/value attributes, there is no need to escape `[]{}` or `:` inside the value. And even for `()` we might improve things. Do you think it is a good idea to let the escaping depend on the element type that is being parsed? Also, most things I will encounter have balanced brackets: when `(` is present, `)` is present as well. We could eg support in a naft attribute that if an opening `(` is encountered that is not escaped, the parser accepts chars until the closing `)` is encountered. Only when this nesting level is back at 0 should the parser close the current attribute on a `)`. Something similar can happen for Tags: only the `[]` actually requires escaping and the same nesting principle can be applied for balanced `[]` brackets.
- [x] `chimp chores` now list all files and for each file, all Chores, ordered in reverse. I want you to order all Chores in reverse (over all files) and during the reporting only show the file when it changes with respect to the file where the previous Chore was found. That should give me all low-order Chores at the bottom of the output with nice collation per file.
- [x] `config.toml` should support the specification of an optional 'default_assignee'. Chores not assigned go to this assignee, if specified. The query running in `chimp chores` must support the specification of one or more assignees via `@name1 @name2`: this should filter the Chores and only report those assigned to 'name1' or 'name2'.
- [x] I want to use TODO, GO, WIP, DONE, QUESTION, INFO, BLOCKED, FORWARD, PLANNED, CANCELED, ASSIGNED as possible status values. These map with checkbox notation `[ ]`, `[*]`, `[/]`, `[x]`, `[?]`, `[i]`, `[!]`, `[>]`, `[<]`, `[-]`, `[~]`. `chimp chores` should only report TODO, GO, WIP, QUESTION and BLOCKED items.
- [x] When a Definition occurs more that once, an ambiguity error must be produced by `chimp check`. It might still make sense to have the same Definition in different locations to start building relative Definitions in different Groves: I have one in the source code repos of my work and the same in my personal second brain. To break the ambiguity, the `^` exclusive marker can be used: if the prime location is marked as `&&^:work`, other locations can start the same 'work' Definition as `&&work` without causing an ambiguity error.
- [x] A Def formatted as `&&@name` is special: it must create a Definition 'name' and but also register 'name' as a potential assignee. When in other locations a Chore is assigned, the specified assignee must be matched against all Defs that can be assignees. When unresolved, `chimp check` must report this. When the same 'name' is defined, `chimp check` should report this ambiguity as well. For other commands, it is OK to simply use one location as the prime location for the Definition.
- [x] The human readable output produced by the different commands must be valid Markdown. Do not add a newline when starting an enumeration. Although this newline is necessary to be Pandoc-compatible, this reads a bit awkward.
- [x] The verbose strategy is as follows, please document it and add relevante logging:
	- 0 means no output except the required one: no warnings, errors etc
	- 1 (default) means enough info on a failure to identify it.
	- 2 can emit more warnings to indicate phishy stuff, but this should not have a significant performance impact
	- 3 mentions each file/folder that is processed
	- 4 can emit a lot of detail
- [x] To be compatible with wikilinks, a `&[[a/b]]` should behave the same as a `&a:b`
- [x] To support the use of spaces inside the different parts of an AmpPath, I want to use backtick-wrapping: do no interpret spaces nor `:` separators inside something that is wrapped in backticks. No need to support escaping backticks: this char will not be used for a AmpPath.
- [x] Commands that report things for files should support the `-e` flag. This should open these files in an editor. `.config/chimp/config.toml` should support the specification of an `editor`. When not specified, the `$EDITOR` env var must be used. When even this is not present, use `nvim`. Do open the first item for each file and indicate the line/col as well. The `-n COUNT` argument, when set, should limit the number of files opened.
- [x] The query mechanism already supports filtering against AmpPath tags and assignees. It should also support additional filtering on the raw text content of each Chore line. Please document how this can be expressed in the query.
- [x] When an AmpPath ends with an `&`, it must invert the dependency injection: instead of relating the AmpPath to the Chore defined on the current line should all the AmpPaths of this line be related to the Definition corresponding to the AmpPath with the trailing `&`. When more than one trailing `&` is present on a line, inject the data in each of them.
	- [x] Wikilinks must support the same trailing `&`
- [x] To reduce false positive AmpPath detection in comments of source code, a comment can only be selected for Amp metadata scanning if it starts with an AmpPath: if it starts with a AmpPath, scan the full comment for additional AmpPaths.
- [x] All AmpPath parts must be case-insensitive: I see no use case in diffentiating between AmpPaths based on casing. Working case-sensitive only increases the risk to miss/mismatch things.
- [x] When a Definition specification ends with a trailing `&`, nested folders and filename (without extension) must create extra Definitions using the folder and filename. This can be stopped by specifying a definition without a trailing `&` in some more nested level.
- [x] If back-to-front resolving of an AmpPath against all Definitions fail, try again but this second time, allow for missing parts: the to-be-resolved AmpPath last part must match with the last part of a Definition, but the second-to-last part (and other parts) of the Definition can be omitted in the AmpPath. Only accept such a partial match if it is unambiguous. Else, report it via `chimp check`.
- [x] Only show Chores where the earliest related date is before-or-equal to the current date: this allows Chores to be specified that should only become visible in the future.
- [x] Date metadata must support an additional offset: `&20260806+1m` must shift the date to 2026-09-06.
- [x] The help for `chimp chores` should provide more details on the query syntax via CLI
- [x] When `@name` is found without a leading `&` it can still be interpreted as an assignee if 'name' matches with a Definition with the extra assignee flag. Do not report resolve issues via `chimp check` on this.
- [x] Metadata should also be parsed from folder and filenames: when a date is identified as `YYYYMMDD` or `YYYY-MM-DD` inside the folder of file name, add the corresponding date to this folder/file and distribute it to its leaf nodes.
- [x] A filename that starts lowercase but contains uppercase letters later should receive the TODO metadata item. Rationale is that denoting a filename as mixed-case initially indicates it is still TODO. When the name is updated later into a full camelcase (with maybe spaces), the file itself Chore is considered DONE.
	- [x] This should only happen in a part of the tree that has the automatic Definition creation based on folder/filename enabled with the trailing `&`; there are too many false positives when this is enabled always.
- [x] An empty AmpPath must be omitted and reported via `chimp check`
- [x] In `src_1` you can find the implementation of a early prototype implemented in Zig. This prototype also support an LSP mode, see `src_1/app/lsp.zig` and some support LSP func in `src_1/rubr.zig`. Can you add a new `chimp lsp` command that supports the same?
- [x] When the `-c FILENAME` is specified, that filename should be used to load the configuration. When unspecified, use `~/.config/chimp/config.toml` and `chimp.toml`.
- [x] Support naft output to a file for the different chimp commands. This structured naft output should be easier to use in end-to-end tests than the current human-readable output. When the `-o FILENAME` is specified, take a look at the extension to determine the output format. Other formats that can be added as well is Markdown.
